# RenderPage: PHP framework

RenderPage is open source.

Requirements
------------

The minimum requirement by RenderPage is a web server running PHP 7.0.

### Install via Composer

```sh
composer require pershin/renderpage:dev-master
```
